# Copier en .env.local si besoin.
# Sur un appareil physique, indiquer l'IP LAN de la machine qui héberge l'API.
# Sans cette variable, l'app déduit l'IP du serveur de dev Expo (hostUri).
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
